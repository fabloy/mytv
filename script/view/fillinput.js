import { name } from "../control/variables.js"

/*fill input assegna al campo input il valore del titolo del film selezionato attraverso il click
nell'autocomplete */
export let fillInput = (filmTitleSelected)=>{
    name.value=filmTitleSelected
   }